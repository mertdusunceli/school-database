@model List<SchoolSystem_Helsinki.Web.Models.Faculty>

@{
    ViewBag.Title = "FacultyList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using SchoolSystem_Helsinki.Web.Models;


<div class="content-header">
    <div class="leftside-content-header">
        <ul class="breadcrumbs">
            <li><i class="fa fa-columns" aria-hidden="true"></i><a href="/Home/Index">Main Page</a></li>
            <li><a>Faculty List</a></li>
        </ul>
    </div>
</div>
<div class="row animated fadeInUp">



    <!--Table-->


    <div class="col-sm-12">
        <h4 class="section-subtitle"><b>Faculty List</b></h4>
        <div class="panel">
            <div class="panel-content">
                <a href="@Url.Action("Create", "Fakultes")" class="btn btn-success">Create</a><br />

                <div class="table-responsive">
                    <table class="table table-striped table-hover table-bordered text-center">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Faculty Name</th>

                                <th>Active</th>

                                <th>Details</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var facultyItem in Model)
                            {

                                <tr>
                                    <td>@facultyItem.Id</td>
                                    <td>@facultyItem.FacultyName</td>

                                    <td>@(facultyItem.Active == (int)EnumHelperData.Satatus.Aktif ? EnumHelperData.Satatus.Aktif : EnumHelperData.Satatus.Pasif)</td>

                                    <td>
                                        <a href="@Url.Action("Edit", "Fakultes",new { id = facultyItem.Id})" class="btn btn-warning">Update</a>
                                        <a href="@Url.Action("Details", "Fakultes",new { id = facultyItem.Id})" class="btn btn-info">Detail</a>
                                        <a href="@Url.Action("Delete", "Fakultes", new { id = facultyItem.Id})" class="btn btn-danger">Delete</a>
                                    </td>
                                </tr>


                            }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>



